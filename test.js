function longestNoSpace(s, ...more)
{
  let maxSpace = 0;

  

}

function countSpace(s)
{
    let countSpace = 0;
    for(let i = 0; i < s.length ; i++)
        if(s[i] === ' ') countSpace++;
        
        
    return countSpace;
}